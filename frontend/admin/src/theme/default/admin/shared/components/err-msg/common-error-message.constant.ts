export const errMsg = {
    required: "{controlName} is required",
    email: "Enter a valid email",
    minlength: "should be allowed a minimum length {requiredLength}",
    maxlength: "should be allowed a maximum length {requiredLength}",
    pattern: 'Please Enter the valid format'
}
