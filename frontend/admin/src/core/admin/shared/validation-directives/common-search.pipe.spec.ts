import { CommonSearchPipe } from './common-search.pipe';

describe('CommonSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CommonSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
