import { NumbersAllowedDirective } from './numbers-allowed.directive';

describe('NumbersAllowedDirective', () => {
  it('should create an instance', () => {
    const directive = new NumbersAllowedDirective();
    expect(directive).toBeTruthy();
  });
});
