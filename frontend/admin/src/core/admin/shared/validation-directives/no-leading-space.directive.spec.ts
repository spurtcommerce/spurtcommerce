import { NoLeadingSpaceDirective } from './no-leading-space.directive';

describe('NoLeadingSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new NoLeadingSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
