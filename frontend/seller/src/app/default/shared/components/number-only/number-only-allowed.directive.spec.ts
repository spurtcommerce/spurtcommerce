import { NumberOnlyAllowedDirective } from './number-only-allowed.directive';

describe('NumberOnlyAllowedDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberOnlyAllowedDirective();
    expect(directive).toBeTruthy();
  });
});
