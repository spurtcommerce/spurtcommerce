import { SellerDynamicDatePipe } from './seller-dynamic-date.pipe';

describe('SellerDynamicDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SellerDynamicDatePipe();
    expect(pipe).toBeTruthy();
  });
});
